# RustSafe 审查报告

## 概要
- **目标：** `src-rust/src/error/` 模块（types.rs, classify.rs, macros.rs, mod.rs）
- **审查时间：** 2026-08-08
- **问题总数：** 10
  - 🔴 高危：1 个
  - 🟡 中等：2 个
  - 🟢 低危：7 个

---

## 🔴 高危问题

### 问题 1：`From<anyhow>` 评分分类是反模式
- **位置：** `classify.rs:27-140`
- **类别：** 错误处理架构
- **描述：** 用字符串匹配评分做错误分类，行为不可预测。例如 `"Config file not found"` 被分类为 `InternalError`（测试 line 247-254 验证了这个反直觉行为）
- **影响：** 错误分类不稳定，新增错误类型时需调整权重，容易引入 bug
- **修复建议：**
```rust
impl From<anyhow::Error> for ErgataiError {
    fn from(err: anyhow::Error) -> Self {
        // 只遍历 chain 找已知类型
        for cause in err.chain() {
            if let Some(e) = cause.downcast_ref::<std::io::Error>() {
                return ErgataiError::IoError(std::io::Error::new(e.kind(), e.to_string()));
            }
            if let Some(e) = cause.downcast_ref::<serde_json::Error>() {
                return ErgataiError::JsonError(e.to_string());
            }
        }
        // 找不到就 fallback
        ErgataiError::InternalError(err.to_string())
    }
}
```

---

## 🟡 中等问题

### 问题 2：公共错误枚举缺少 `#[non_exhaustive]`
- **位置：** `types.rs:9-10`, `types.rs:96-97`
- **类别：** API 设计
- **描述：** `ErgataiError` 和 `ConfigError` 没有 `#[non_exhaustive]`，未来添加变体是 breaking change
- **影响：** 未来扩展错误类型时，所有 `match` 这些枚举的代码都会编译失败
- **修复建议：**
```rust
#[non_exhaustive]
#[derive(Error, Debug)]
pub enum ErgataiError { /* ... */ }

#[non_exhaustive]
#[derive(Error, Debug)]
pub enum ConfigError { /* ... */ }
```

### 问题 3：错误变体缺少结构化上下文
- **位置：** `types.rs:13-87`
- **类别：** 错误设计
- **描述：** `AgentNotFound(String)`, `AgentTimeout(String)` 等丢失结构化信息，前端无法条件渲染，日志分析需解析字符串
- **影响：** 前端无法根据错误字段做条件渲染，日志分析需要解析字符串，调试困难
- **修复建议：**
```rust
#[error("Agent not found: {agent_id}")]
AgentNotFound { agent_id: String },

#[error("Agent timeout after {duration_ms}ms: {operation}")]
AgentTimeout { duration_ms: u64, operation: String },

#[error("Session not found: {session_id}")]
SessionNotFound { session_id: String },
```

---

## 🟢 低危问题

### 问题 4：`ConfigError` 分类是死代码
- **位置：** `classify.rs:110-113, 136`
- **类别：** 代码清理
- **描述：** 评分匹配 `ConfigError` 但映射到 `InternalError`，注释写 "deprecated path"
- **修复建议：** 删除或正确映射到 `ErgataiError::ConfigError(...)`

### 问题 5：`with_context!` 宏重复 anyhow 功能
- **位置：** `macros.rs:20-41`
- **类别：** 代码清理
- **描述：** anyhow 的 `.context()` 完全覆盖此宏
- **修复建议：** 删除宏，改用 `anyhow::Context`

### 问题 6：`retry_transient!` 宏未使用
- **位置：** `macros.rs:55-82`
- **类别：** 代码清理
- **描述：** grep 无调用点
- **修复建议：** 删除（YAGNI）

### 问题 7：`ConfigError::ValidationFailed` 缺少字段名
- **位置：** `types.rs:115-116`
- **类别：** 错误设计
- **描述：** 只有 `reason` 没有 `field`，前端无法高亮具体字段
- **修复建议：** 添加 `field: String` 字段

### 问题 8：`error_code()` 返回字符串而非 enum
- **位置：** `types.rs:123-165`
- **类别：** API 设计
- **描述：** 字符串容易拼错，IDE 无法补全
- **修复建议：** 返回 `ErrorCode` enum

### 问题 9：缺少错误严重程度字段
- **位置：** `types.rs` 整体
- **类别：** 错误设计
- **描述：** `is_user_error()` / `is_transient()` 是布尔方法，缺少统一的 severity 字段
- **修复建议：** 添加 `severity()` 方法返回 `ErrorSeverity` enum

### 问题 10：`scores` Vec 未预分配容量
- **位置：** `classify.rs:45`
- **类别：** 性能优化
- **描述：** `Vec::new()` 然后最多 push 11 次，可能重新分配 3-4 次
- **修复建议：** `Vec::with_capacity(11)`

---

## 审查总结

**设计优点：**
- 单一 `ErgataiError` enum 统一所有错误，NAPI 边界 `From` 转换一致
- `error_code()` / `is_user_error()` / `is_transient()` 提供前端分类和重试决策
- `ConfigError` 细化配置错误，高频路径有专门处理
- 无所有权/并发安全问题

**主要问题：**
- 评分分类是过度工程化，行为不可预测（高危）
- 缺少 `#[non_exhaustive]`，未来扩展是 breaking change（中等）
- 错误变体用 `String` 丢失结构化信息（中等）
- 两个宏是 YAGNI，应删除（低危）

**无问题领域：**
- 所有权与生命周期 ✓
- 并发安全 ✓
- unsafe 使用 ✓
- 枚举大小和内存布局 ✓
