# 修复计划

## 执行顺序

### 第一步：修复中等问题（建议尽快修复）

#### 1.1 简化 `From<anyhow>` 分类逻辑 + 标记 TODO
- **文件：** `src-rust/src/error/classify.rs:27-140`
- **修复类型：** 架构调整
- **修复步骤：**
  1. 删除评分分类逻辑（line 44-138）
  2. 在 chain 遍历中添加 `ConfigError` downcast
  3. 保留 fallback 到 `InternalError`
  4. 添加 `// TODO: 考虑是否需要更多类型的 chain downcast` 注释
- **修复代码：**
```rust
impl From<anyhow::Error> for ErgataiError {
    fn from(err: anyhow::Error) -> Self {
        // 遍历 chain 找已知类型
        for cause in err.chain() {
            if let Some(e) = cause.downcast_ref::<std::io::Error>() {
                return ErgataiError::IoError(std::io::Error::new(e.kind(), e.to_string()));
            }
            if let Some(e) = cause.downcast_ref::<serde_json::Error>() {
                return ErgataiError::JsonError(e.to_string());
            }
            if let Some(e) = cause.downcast_ref::<ConfigError>() {
                return ErgataiError::ConfigError(e.clone()); // 需要 ConfigError: Clone
            }
        }
        // TODO: 考虑是否需要更多类型的 chain downcast
        // Fallback 到 InternalError
        ErgataiError::InternalError(err.to_string())
    }
}
```
- **验证方法：** 运行现有测试，删除 `test_anyhow_conversion_string_classification` 中依赖评分分类的断言
- **风险：** 调用方可能依赖当前的字符串分类行为，需要检查所有 `anyhow::Error` 转换为 `ErgataiError` 的调用点

#### 1.2 添加 `#[non_exhaustive]` 到公共错误枚举
- **文件：** `src-rust/src/error/types.rs:9-10, 96-97`
- **修复类型：** API 防御
- **修复步骤：**
  1. 在 `ErgataiError` 和 `ConfigError` 上添加 `#[non_exhaustive]`
  2. 确保所有 `match` 语句有 `_` 分支（编译器会提示）
- **修复代码：**
```rust
#[non_exhaustive]
#[derive(Error, Debug)]
pub enum ErgataiError {
    // ... 现有变体
}

#[non_exhaustive]
#[derive(Error, Debug)]
pub enum ConfigError {
    // ... 现有变体
}
```
- **验证方法：** `cargo check` 确认编译通过
- **风险：** 低 — 现有 `match` 语句可能已经有 `_` 分支（编译器会验证）

---

### 第二步：修复低危问题（可选，代码清理）

#### 2.1 删除 `with_context!` 宏
- **文件：** `src-rust/src/error/macros.rs:20-41`
- **修复类型：** 代码删除
- **修复步骤：**
  1. 删除 `with_context!` 宏定义
  2. 删除相关测试（line 88-116）
  3. 搜索代码库确认无调用（已知无调用）
- **验证方法：** `cargo check` 确认编译通过
- **风险：** 无 — 无调用点

#### 2.2 删除 `retry_transient!` 宏
- **文件：** `src-rust/src/error/macros.rs:55-82`
- **修复类型：** 代码删除
- **修复步骤：**
  1. 删除 `retry_transient!` 宏定义
  2. 删除相关测试（如有）
  3. 搜索代码库确认无调用（已知无调用）
- **验证方法：** `cargo check` 确认编译通过
- **风险：** 无 — 无调用点

#### 2.3 清理 `ConfigError` 死代码
- **文件：** `src-rust/src/error/classify.rs:110-113, 136`
- **修复类型：** 代码删除
- **修复步骤：**
  1. 删除 `add(ErgataiErrorClass::ConfigError, ...)`（line 110-113）
  2. 如果删除整个评分分类（见 1.1），此问题自动解决
- **验证方法：** `cargo check` + `cargo test`
- **风险：** 低 — 不影响行为

#### 2.4 重命名 `log()` 方法避免遮蔽
- **文件：** `src-rust/src/error/types.rs:190`
- **修复类型：** 重命名
- **修复步骤：**
  1. 将 `log()` 重命名为 `emit_log()` 或 `log_self()`
  2. 更新所有调用点（已知只有 `classify.rs:12` 调用）
- **修复代码：**
```rust
pub fn emit_log(&self) {
    if self.is_user_error() {
        tracing::warn!(/* ... */);
    } else if self.is_transient() {
        tracing::info!(/* ... */);
    } else {
        tracing::error!(/* ... */);
    }
}
```
- **验证方法：** `cargo check` + `cargo test`
- **风险：** 低 — 只有一个调用点

---

## 修复验证清单

- [ ] 所有中等问题已修复
- [ ] 所有低危问题已修复或标记为"接受风险"
- [ ] 代码编译通过（`cargo check`）
- [ ] 所有测试通过（`cargo test`）
- [ ] Clippy 检查通过（`cargo clippy`）
- [ ] 无新引入的问题

---

## 执行建议

**优先级：** 第一步（中等问题）建议尽快修复，第二步（低危问题）可在下次代码清理时处理。

**风险评估：**
- 1.1（简化评分分类）风险最高 — 需要确认调用方不依赖字符串分类行为
- 1.2（添加 `#[non_exhaustive]`）风险低 — 编译器会验证
- 2.1-2.4（代码删除和重命名）风险低 — 无调用点或单调用点

**不修复的项：**
- 原问题 3（错误变体缺少结构化）— 误报，现有设计合理
- 原问题 7/8/9 — 误报，现有设计合理
- 原问题 10（Vec 预分配）— 误报，非热路径
